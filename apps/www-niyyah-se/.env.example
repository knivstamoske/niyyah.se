# Node environment
NODE_ENV=production

# Database URL for local PostgreSQL (matches credentials in root .env)
DATABASE_URL="postgresql://niyyah:niyyah@localhost:5432/niyyah?options=-c%20search_path%3Dmarketing"

# Application Origin URL (required for production)
ORIGIN=http://localhost:8050
